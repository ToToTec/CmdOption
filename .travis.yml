sudo: false

language: java

jdk:
  - oraclejdk8

addons:
  apt:
    packages:
    - gettext

before_script: 
- mvn -N io.takari:maven:wrapper -Dmaven=3.5.0
- xgettext --version

script: 
- ../mvnw clean install -Prelease
